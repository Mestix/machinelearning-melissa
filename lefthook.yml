pre-commit:
  parallel: true
  commands:
    notebook-clean:
      run: nbstripout /notebooks/*ipynb
    black:
      run: black networks/ dev/
    ruff:
      run: ruff check networks/ dev/ --fix
    mypy:
      run: mypy networks/ dev/